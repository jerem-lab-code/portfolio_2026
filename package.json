{
  "name": "portfolio-jk-2026",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173 --strictPort",
    "build": "vite build && npm run postbuild:static",
    "postbuild:static": "node scripts/postbuild-copy-static.mjs",
    "preview": "vite preview --host 0.0.0.0 --port 4173 --strictPort"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
